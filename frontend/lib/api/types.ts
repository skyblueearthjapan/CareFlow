/**
 * Placeholder OpenAPI paths type.
 *
 * TODO: Generate concrete `paths` / `components` types via
 * `openapi-typescript` against `backend/openapi.json` and replace this
 * file. Keeping a permissive `Record<string, unknown>` here lets
 * `openapi-fetch` compile while we wire up the rest of Phase 2.
 */
export type paths = Record<string, unknown>;

export type components = Record<string, unknown>;
