# 引き継ぎ書：2026-07-09〜10 セッション総合（連携ページ刷新・スタッフ退職事故の根治・PFV正の設計転換）

作成 2026-07-10 / **★最新状態は §12（2026-07-26追記）: カイポケイベント取り込み E-0〜E-3 実装完了**
（§11=モニター地図 2026-07-13。4段ソルバ v2.0 は §10。§3以前のHEAD/DB記述は履歴）。
**次のエージェントはまずこのファイル（§12→§11→§10→§9→§8の順）を読む。** 前正典 = `docs/plans/session-2026-07-08b-HANDOFF.md`（テーブル撤去・PW締め付け・RBAC統一まではそちら）。

---

## 0. 今日を一言で

**「関谷さん退職」を起点に設計矛盾が露呈 → PO と設計原則を確定して根治した日。**
あわせて連携ページを全面的に作り替え、共通パスワード運用の残件（s003無効化・髙梨アカウント）も完了。

## 1. 確定した設計原則（PO明言・今後の全判断基準）★最重要

1. **PFV（固定訪問スケジュール）のコースが「正」（需要側）**
2. **スタッフマスタは供給側（充足チェック）。スタッフはコースに後から紐づける**
3. **両者のズレは隠さず警告**（スタッフ不足バナー・（担当不在）表示）
4. **PFV がスタッフ数を超えても警告のみ・作成はブロックしない（案α・PO確定 2026-07-10）**
5. **コードが特定の事業所/スタッフを認識しない。すべてマスタ駆動**（登録されれば使われ、消えれば使われない）
6. **表示のスタッフ正典 = `courses.assigned_staff_id`。`visits.primary_staff_id` はそのミラー（レガシー互換）**

→ 詳細メモリ: `careflow-staff-assignment-source.md`

## 2. 本日やったこと（全コミット・すべて本番稼働）

### A. 連携（カイポケ）ページの全面刷新
| commit | 内容 |
|---|---|
| `7d8f87d` | 左右2カラム化＋**接続設定を右上「設定」→ダイアログへ格納**（認証情報を表側に出さない。閉時はDOMに無い） |
| `d6d6a58` | 取り込み枠の高さを左に揃える |
| `ac41939` | **コンソール型へ再構成**: 上=モニター＋操作(週次反映/取り込み)、下=全幅タブカレンダー(送る/取り込みプレビュー)。useWeeklyApply/useInbound フック抽出・ロジック完全不変（レビューで確認） |
| `15668c4` | 稼働状況をモニター直下へ非圧縮で戻す＋カレンダー min-h-[460px]（下段が画面外に押し出されて「週の予定が見えない」報告の解） |
| `843de11` | 取り込みカードの下辺を稼働状況カードに揃える（flex-1） |
| `561a626` | **「この週の予定」が空になるバグ根治**: get_week_schedule が legacy primary_staff_id 必須で全スキップしていた → 正典ソース(①VSA ②courses.assigned ③legacy)解決へ。W28: 1→127行 |
| `2bb4d0e` | 連携のスケジュール表示（週予定/差分/取り込みプレビュー）を**性別カードUIへ意匠統一**（patientSex をBE加算＋patient_id FE join。実測 female82/male45） |

### B. スタッフ割当の同期漏れ根治（3連鎖）
| commit | 内容 |
|---|---|
| `8ae82a4` | **週生成の自動割当が visits.primary_staff_id を書いていなかった**（VSAのみ）→ 同期修正＋本番backfill |
| （DB操作） | backfill は当初 VSA 優先で実施→**モニターとスケジュールの担当対応が総崩れ（私の取り違え）**→ `courses.assigned_staff_id` 優先で再backfill（720件・残ミスマッチ0）。7/9 モニター=スケジュール完全一致を実測 |
| `937f511` | **PATCH /courses（担当スタッフ変更ドロップダウン）が visits.primary を伝播していなかった**（ズレの再発源）→ 一括伝播（手動上書き尊重） |
| `4e99de1` | モニターの行順を「**拠点→コース(A,B,..)→スタッフ名**」へ（旧: スタッフ名順でコース順不同に見えた） |

### C. 関谷さん退職事故（7/9 11:59 スタッフ論理削除）と設計転換
- **症状**: スケジュールから稲毛C/D列が消滅（A-E開講判定が稼働スタッフ数連動のため）。訪問43件が管理画面から不可視（モニター/モバイル/連携には出る）
- **PO自身の復旧操作**: 22:03 一斉未割当(成功)→「週を生成」**500失敗**（後述の既知バグ）
- **復旧**: `assign-staff-only`（自動スタッフ割当）を代行実行 → 26コース確定・primary同期・モニター/モバイル正常化
- `9910ff8` **【設計転換P1】コース列表示の正=PFV**: 表示 = 従来開講判定 ∪ **PFV presence** ∪ **当該日visit実在**。⚠スタッフ不足バナー・（担当不在）琥珀表示。新API `GET /v2/pfv-course-presence`。週一覧の「休」セルに訪問が隠れる問題も解消
- **案α確定**（PO 2026-07-10）: 上記警告方式で完成。**追加実装なし**

### D. アカウント（共通パスワード運用の続き）
- **s003（関谷さん）ログインアカウント無効化**（論理削除・ログイン401実測。スタッフ削除とアカウントは別物だった）
- **髙梨桂子さん（S007）**: アカウント未発行だった → provision_staff_accounts.py で発行→共通PWハッシュへUPDATE→must_change=false→ログイン200実測→**ローカルの staff-login-cards.html に S007 カード追加済（gitignore対象・ローカルのみ）**

### E. 誤操作対策・マスタ駆動化（`ec7dfef`・migration 0059）
- **「週を生成」再実行確認ダイアログ**: 訪問がある週で押すと「この週は既に生成されています。再実行しますか？」＋固定枠に戻すへの誘導。0件の週は従来どおり即実行
- **拠点直書き2箇所のマスタ駆動化**: offices に sort_order/short_label（稲毛=1/稲・都賀=2/津 backfill済）。FieldBoard の定数撤去・Excel取込(schema.py)は純粋パーサのまま対応表を注入設計に（legacy値はfallback残置）

### F. 調査で確定した事実（コード根拠つき・再調査不要）
1. **「週を生成」(Layer1) は最初からPFV正**（PFVの course_template_id どおりコース生成。スタッフ数無関係。layer1_expander.py:1075-1106/293-346）
2. **常用の提案エンジン（プール投入提案/範囲最適化/一括投入）は既に「その週の生成済み全コース」母集合**（load_week_course_buckets 共通。スタッフ数フィルタなし・担当不在コースも候補・警告＋スコア降格）＝POの希望像どおり。**作り替え不要**
3. スタッフ数でコース本数を絞るのは (a)auto_allocator_v2 Stage5（新規セットのコード発番。常用外） (b)画面表示（P1で是正済み）のみ。**「全体最適化」ボタンは廃止済み**（監査ログの94回は廃止前）
4. **generate-week-only 500 の根因**: 冪等削除=planned+auto のみ／衝突ガード=manual のみ保護 → **completed になった auto 訪問**が残ったまま同キーINSERT → uq_visits_pds_group_active 違反。**実施済み訪問がある週への再生成は必ず500**（ロールバックされデータは無傷）。運用は「固定枠に戻す」で代替＋確認ダイアログで誤操作防止（POの選択。バグ自体は未修正）
5. 稼働カウント(count_active_staff_per_weekday)= role='staff'・trainee除外・削除除外。**川名/熊澤=manager、髙梨=trainee なので稲毛の稼働カウントは実質2名**（宇田川・高岡）→ 不足バナーが常時出る可能性あり（下記「気になる点」）

## 3. 残タスク（明確に）

### 次に手を付けるならこれ
- **（PO実機確認待ち）** 本日分の確認: ①スケジュールにC/D列が出る＋⚠不足バナー＋（担当不在）表示 ②「週を生成」ダイアログ ③連携ページ新レイアウト/カードUI/週予定127件 ④モニター並び順 ⑤モバイル「今日の訪問」がスタッフに出る ⑥髙梨さんログイン ⑦現場ボードの拠点順
- **不足バナーの分母問題（設計確認）**: 稼働カウントが manager/trainee 除外のため「コース5本に対して稼働2名」が恒常表示になり得る。POが煩わしいと感じたら「カウントに manager/trainee を含めるか」「バナーの条件調整」を相談（コード1箇所: count_active_staff_per_weekday の絞り or バナー判定）

### バックログ（急がない）
- generate-week-only 500 バグ修正（根因既知=ガードのキー集合を「冪等削除で残る全active visit」へ拡張。運用回避済みのため優先度低）
- auto_allocator_v2 Stage5（新規発番の人数基準）: 案α原則との整合ウォッチのみ。実害報告なし
- W42サンドボックス（10月）の courses.assigned に関谷さん残存 → 生成し直しで解消可。過去週(W21/W27)は履歴として正
- D-10: e2e(Playwright) を vitest 設定で除外（現状 `--exclude e2e/**` 手動運用）
- D-11: BulkPoolInsertDialog のフルスイート時間欠フレーク（単体では常にpass）
- 前正典(08b)の B系（カイポケ初回実apply=PO監督必須）・C系（GEMINI失効/カイポケPWローテ/untracked 2ファイル）・D系 は据え置き

## 4. 気になる点（次のエージェントがハマりそうな所）★必読

1. **デプロイの罠**: docker build がキャッシュで**新規ファイル（特に alembic migration）を取り込まないことがある**。今回 0059 が2回目のビルドまでイメージに入らなかった。**migration や新規ファイルを含むデプロイは `build --no-cache`**。手順: pg_dump → pull → build(--no-cache) → `alembic upgrade head`（up の前）→ up -d --force-recreate → healthz 内外
2. **スタッフ割当の書き込みは必ず「VSA と visits.primary_staff_id」をペアで**。片方だけ書く/消すと モニター・モバイル・ダッシュボード等6画面（visits.py:53 ほか）が壊れる。解除側(2026-07-03)・付与側(8ae82a4)・担当変更(937f511)の3事故すべて同根
3. **「週を生成」は実施済み訪問がある週で必ず500**（仕様として残置・ダイアログで防護）。誤って案内しないこと。やり直しは「固定枠に戻す」
4. **共通パスワード運用**: 新スタッフは provision_staff_accounts.py → 共通PWハッシュへUPDATE + must_change=false（手順はメモリ careflow-unified-password.md）。**退職時はスタッフ削除とは別に users の無効化が必要**（今回の学び）
5. offices.sort_order/short_label の**編集UIは無い**（DB/API直のみ）。新拠点追加時は PATCH /offices か SQL で設定（無設定でも名前順/先頭1文字で動く）
6. staff-login-cards.html は **gitignore（ローカルのみ）**。S007追加済み。リポジトリには無いのでマシン移行時に注意
7. 監査ログの機能利用実績には**開発中の動作確認の呼び出しが混ざる**。「使っていない」判定は慎重に
8. 連携ページの既定週は「来週」。未生成なら空表示=正常（「◀前の週」で生成済み週へ）
9. FEユニットテストの基準値: **1116 pass / 0 fail**（`pnpm vitest run --exclude "e2e/**"`）。BE は既存フレーク（test_integration_kaipoke の diff/apply/patch 3件＋teardown、patients_v2 の2件）があり**元コードでも落ちる**——新規failと混同しない

## 4.5 受け入れ枠○×とスケジュールの連動調査（2026-07-10・PO質問への回答）

**結論: 連動している（リアルタイム・キャッシュなし）**。
- 算出源 = その週の**確定コース（status≠proposed）× planned訪問の「患者数と時刻」のみ**
  （acceptance_matrix_service.py:387/400-427/478-490）。訪問の追加/移動/削除は即○×へ反映。
  実測: 都賀木曜の○×が本名氏の実訪問(11:00/15:00/16:00=×・空き時間=○)と1対1で一致。
- **スタッフ非依存**: courses.assigned_staff_id も visits.primary_staff_id も読まない
  → 今週の退職・担当変更・backfill の影響を一切受けていない（健全）。
  稼働スタッフ数の開講判定も不使用 → **PFV正の原則と整合**（C/Dコースの空きも数える）。
- 手動上書き（この週だけ>毎週>自動）の3層合成は従来どおり。
- **注意（案αの帰結として仕様どおりだがPO周知済みにすべき点）**: ○×は「枠と時間の空き」だけを
  見るため、**担当スタッフが居ないコースの空きも○に数える**。スタッフ充足はスケジュール画面の
  警告（不足バナー/担当不在）側の責務、という分担。
- 任意バックログ: 「スタッフ変更→○×不変」の回帰テスト追加（acceptance_matrix）。

### ★受け入れ枠バグ修正（2026-07-10・`6db1188` 本番稼働）
- **症状(PO報告)**: 月曜稲毛の A-D が全て満床(6-7名)なのに受け入れ枠が○（各時間帯1名空きに見える）。
- **原因**: 受け入れ枠の母集合に**空の M コース(course_fixed・患者0)が含まれ**、`remaining = 6 - 0 = 6`
  を提供 → 拠点に M が1本あるだけで A-D 満床でも全時間帯○になっていた（実測: 月曜稲毛 A/B/C/D=残0・M=残6）。
  **全拠点・全曜日で同様**に発生していた。
- **修正(案1・PO決定)**: `acceptance_matrix_service.py` の course 母集合から **M系(M/M2..M9)を除外**
  （`_is_manager_course` ヘルパー）。M=マネージャー予備枠で「新規受け入れ先になり得ない」。
  **臨時コース(臨..)は実訪問を持つため除外しない**。単体テスト追加(accept 22pass)。
  修正後 月曜稲毛=全時間帯×/残0 を本番実測で確認。
- 教訓: 受け入れ枠の「コース母集合」は表示側(P1のPFV正)とは別実装。**M系の扱いを両者で意識**。

## 4.6 らく助リブランディング（2026-07-10・本番稼働 HEAD `879d079`）

**PO決定: アプリを「CareFlow」→「らく助（訪問看護 楽々スケジュール）」へ改称・マスコット導入。**
デザインシート正 = `Sampledata/らく助／アイコン最終.png`（1536x1024）。表記は「訪問看護」でOK（PO確認済）。

- **方針 = ハイブリッド配色**（AskUserQuestionでPO選択）: ブランド層（primary系）のみピンクへ、
  機能層（性別/状態/警告/info/現場ボードCF_THEME）は据え置き。
  `tokens.css`: brand-primary `#e15a7f`（シート原色#f8b4c6から白文字コントラスト確保のため導出）/
  hover `#c94a6e` / light `#ffd6e0` / 50 `#fff5f8`。identity `#f8b4c6`・leaf `#78c888` 追加。
- **コミット**: `e779781`(R-1基盤: 配色/名称/アイコン/ログイン) `547ec8b`(R-2モバイル: 挨拶横マスコット+空状態)
  `879d079`(docsモック+素材18点)。frontendのみ・migration無し。`build --no-cache` でデプロイ・healthz内外OK。
- **素材パイプライン**: `Sampledata/extract_rakusuke.py`（シート→透過PNG18点切り出し。Codexに指示して作成、
  座標は3回目視調整）→ `docs/mockups/assets/rakusuke/` → 使用5点を `frontend/public/brand/` へ。
  PWA/favicon生成 = `frontend/scripts/gen-icons.py`（らく助タイル合成へ書き換え。favicon=app/icon.png 初設置）。
- **合意モック** = `docs/mockups/rakusuke-brand-mock.html`（パレット/3画面/アイコン/論点）。
- **ハマり所**: middleware.ts の matcher に `brand/`・`icon.png`・`apple-icon.png` を除外追加必須
  （無いとログイン画面のロゴ画像が認証リダイレクトで読めない）。
  MonitorMapClient.tsx はSVG直書き3箇所を brand-primary と手動同期（コメントどおり実施済）。
- **R-3（同日追補・`97c80f2` 本番稼働）**: ①theme_color（ブラウザ/PWAトップバー）3箇所を
  #e15a7f→**#f8b4c6**（PO要望「薄く上品に」）②モバイル上部バー淡ピンクウォッシュ化
  ③**現場ボード+受け入れ枠(/m系)もブランド統一**: CF_THEME.TEAL/TEAL_DEEP→#E15A7F/#C94A6E
  （theme.ts一点・キー名TEALのまま。医療青/介護緑の保険種別色は直書きで不変）+field.cssフォーカス
  +BackToAppBarにらく助マーク ④**ロゴ「ら」欠け修正（PO指摘）**: 矩形消去→左端接触かつy<270の
  連結成分のみ除去（Codex改修・extract_rakusuke.py）。
- **R-4（同日追補・`37602cf` 本番稼働）マスコット全面展開**: 共通コンポーネント
  `components/brand/Rakusuke.tsx`（Rakusuke素画像 / RakusukeNote空状態ブロック・ポーズ10種）を新設し
  **PC/モバイル17箇所**へ配置。配置原則=「状態の瞬間（空/完了/エラー/確認）に出す・高密度グリッドに常駐させない」。
  モバイル: 今日/今週/マイページ/通知の空状態・訪問完了(cheer)・打刻中(visit)。
  PC: ダッシュボードEmptyState(pose prop内蔵)・患者/スタッフ/拠点/ユーザー/監査ログ/申請履歴の空状態・
  カイポケ差分なし(cheer)/週予定なし(calendar)・週次ガイド/週生成確認ダイアログ・全画面エラー境界(puzzled)。
  **今後らく助を出すときは必ずこのコンポーネント経由で**（直 img 禁止・ポーズは意味で選ぶ）。
- **据え置き（次回以降）**: ①QR印刷物（患者宅掲示の対外物・PO確認後に改称）
  ②性別カード女性ピンクとUIピンクの識別チューニング（PO実機の感想待ち）
  ③スタッフ配布のログインカード staff-login-cards.html のブランド更新（ローカル）。
- **ロールバック**: `git revert 97c80f2 547ec8b e779781` → push → frontend再ビルドで完全復元（DB無関係）。

## 4.6b らく助 R-5/R-6 追補（`3458c3b`/`8cf0b8c` 本番稼働）
- **モニター外殻のUI統一**（PO指摘「モニターだけUIが違う」）: タイトルを箱の外・text-2xlへ、
  サブタイトル追加、高さ calc(100vh-100px) 廃止→スケジュールと同じ lg:h-full 方式（約36pxはみ出し根治）。
- **R-6 全ページタイトルにらく助**（`RakusukeTitle` 新設・PC版19ページ・Codexが仕様書
  `Sampledata/rakusuke-title-spec.md` に沿って一括適用→検品）。ポーズ選定はコミットログ参照。
  **新ページを作るときはタイトルを RakusukeTitle で書く**（h1直書きしない）。

## 4.6c らく助 R-7 モバイル系（`6d3aba7` 本番稼働）
- 受け入れ枠(/m/acceptance): ヘッダ帯=淡ピンク×深ピンク文字・エリア帯=芽グリーン淡緑×深緑文字へ
  （両帯を同濃度の上品トーンに。旧: 濃ピンク/濃緑の塗り帯）。ヘッダにらく助(heart)。
- MobileSection に pose プロパティ追加 → 今日(visit)/今週(calendar)/マイページ(clap)。
- **調査結果（PO懸念への回答）**: 受け入れ枠の拠点トグルは offices API を map（拠点数任意・
  `offices.length > 1` で表示・flex:1均等割）。地名ハードコード無し=他事業所展開OK。
  残骸: `components/field/theme.ts` の型 `OfficeKey = 'INAGE'|'TSUGA'` は未使用の型定義のみ
  （実挙動に影響なし・掃除はバックログ）。5拠点以上で横幅が窮屈になったら折返し検討。

## 4.6d らく助モバイル R-8（`afaf3d2` 本番稼働・PO決定）
- **モバイル3面の統一**: 傘名「らく助モバイル」・タブ=ホーム(/m/home系)/ボード(/m)/受け入れ枠(/m/acceptance)。
- 新設 `components/mobile/MobileSurfaceSwitcher.tsx`（らく助マーク+タブ型ピル・現在地ハイライト・
  usePathname判定）を3面の最上部に共通配置。「現場ボードを開く」「←戻る」「←現場ボードへ」の
  バラバラ導線を全廃。ボードの操作バー(患者管理/提案/承認)は canEdit のみ表示に。
- 訪問詳細の戻るは「← 今日の訪問に戻る」テキストピル（`4057ce3`）。
- **テストの罠**: field の page.test.tsx は next/navigation をモックしており、switcher導入で
  usePathname の追加が必要だった（29件全滅→モック1行で復旧）。旧戻りリンクの検証3箇所も
  「画面切替」検証へ更新済み。モバイル導線を変えるときはこのモックに注意。

## 4.6e らく助 R-9（`5ce0d37` 本番稼働）モバイル訪問カードの性別ウォッシュ統一
- 今日の訪問/今週の予定のカードを PC版と同じ視覚言語へ（地色=患者性別ウォッシュ・
  左帯/性別ドット・時刻tnum・📍住所）。共通 MobileVisitCard を刷新。
- BE: visits 一覧/詳細射影に patient_sex/patient_address を非破壊追加（VisitRead拡張）。
- 既知: tests/scripts/test_cleanup_w16_course_code_mismatch.py は収集エラー（_apply_fix
  import不整合・既存）。BE test_visits の sqlite "cannot commit" は再実行で消えるフレーク。

## 4.7 訪問モニター行のコース単位化＋コース⇔訪問担当の乖離調査（2026-07-10午後・`024217f` 本番稼働）

**PO報告「稲毛A-D予定があるのにA,Bまでしか映らない」の調査と改修。**

### 調査で確定した事実（監査ログ+DB実測・再調査不要）
1. **旧モニターは「訪問担当1人=1行」**。稲毛4コースを実質2名（宇田川/高岡）で回すと2行に潰れて見えた。
2. **コース担当と訪問担当が乖離していた**。7/10朝のPO操作: 04:44一斉未割当→**04:47自動スタッフ割当が7秒差で2回**
   （2回目がコース担当を髙梨/熊澤/川名/高岡に上書き）→**06:45「固定枠に戻す」が訪問側だけPFV由来（宇田川/高岡）に再構築**し
   コース側を残置。→ ハマり所2の**第4の事故経路 = reset-to-fixed の同期漏れ**（過去3件: 解除側/付与側/担当変更）。
3. assign-staff-only は**再実行で別解を出す非冪等性**あり（2回目は1回目の割当済みスタッフを busy 扱いで除外→manager/trainee に流れる）。

### 改修（`024217f`）
- **行=コース単位へ**（build_monitor のグルーピングを course_id 軸に）。掛け持ちはコースごとに行が分かれ、
  1コース2名は1行に集約（staff_ids/連結staff_name）。行の拠点は**コースのoffice_id由来**に変更。
- **MonitorVisit に staff_id/staff_name 追加 = visits.primary_staff_id が源泉**。モバイル「今日の訪問」と
  同一ソースのため、モニターに出る担当とスタッフ端末の表示は構造的に一致（PO要望「モバイルとの紐づけ」）。
- 次訪問距離は行内でも同スタッフチェーン単位を維持。イベント帯は行内全担当分。詳細パネルは訪問単位の担当を優先表示。
- コース単位化により**表示上はコース⇔訪問乖離の影響を受けない**（行はvisits由来のみで構成）。

### 追補（`17a8f33` 本番稼働）: スケジュール側コース担当の表示＋⚠突合
- PO要望「スケジュールに割当があるなら訪問モニターにも表示」→ 行に
  **course_staff_id/course_staff_name（=courses.assigned_staff_id・スケジュール画面と同一ソース）**を追加。
  行ヘッダ/詳細パネルの担当表示はコース担当優先・実訪問の担当と食い違う行は**⚠表示**
  （原則③。reset-to-fixed同期漏れ等の乖離が現場で即発見できる）。

### 残課題（バックログ）
- **W28乖離は修復済み（2026-07-10 21時・PO承認の上で代行実行）**: pg_dump→admin権限で
  assign-staff-only W28を1回実行（200・25コース確定/1件review）。熊澤モバイル=今日6件/週36件を実測。
  残ミスマッチは7/6完了済み訪問1件（担当欄空の過去履歴・実害なし）のみ。
  ※症状: 熊澤さんのモバイルが0件（スケジュール表示はコース担当・モバイルは訪問担当を読むため）。
- 根治: ①reset-to-fixed 実行時に courses.assigned をリセットor訪問側へ同期 ②assign-staff-only の
  二度押しガード（実行中ロック or 直近実行の解を再利用）。

## 6. 次セッションの開始手順（2026-07-10深夜 追記・最優先=Cloudflare）

**最初にやること: Cloudflare Access問題の根治**（POが毎日ログインを求められ、PWA版アプリで
ライブモニター/APIが401になる。本番HEAD `96b825c` 時点の症状・経緯は §「LiveMonitorCard」コミット群参照）
1. **POとCloudflareダッシュボードを開き**: Zero Trust → Access → Applications で
   `carelink.kaipoke-api.net` / `novnc.kaipoke-api.net` の **Session Duration を延長**（推奨: 1週間〜1か月）。
   保護パスの範囲（/api を含むか）とアプリ分割も確認。
2. **GASとの違い（PO質問への回答・確定済みの理解）**: 旧GAS構成はGoogleサーバーから
   カイポケへ直接通信しておりAccessの「人間ログイン」を通らなかった。現構成は
   carelink/novnc/kaipoke-api を**人間用Accessで保護**しているためセッション期限に引っ張られる。
   機械間通信は **Access Service Token（サービス認証）** に切り替えればログイン不要にできる。
3. **アプリ内「Cloudflareに再ログイン」導線**: APIが401(Access起因)のとき画面上部バナー→
   **トップレベル遷移**で認証フローへ（PWAはアドレスバーが無く自力復旧できないため必須）。
   注意: iframe内へのAccessログインは frame-ancestors 'none' で不可能。必ず窓ごと遷移させる。
4. ライブモニター探針の誤検知根因（novnc の app/images/connect.svg が実在するか）確認。

**その他の残タスク（優先順）**: ①ESLint未使用import掃除（field系/this-week等・ビルド警告）
②theme.ts の OfficeKey型残骸削除 ③reset-to-fixed のコース担当同期漏れ根治＋assign-staff-only
二度押しガード（§4.7） ④R-10文言規約に「0件のとき断言しない」を設計書へ追記
⑤tests/scripts/test_cleanup_w16 収集エラー修正 ⑥BE test_visits sqliteフレーク・FE D-11フレーク据え置き

**このセッションの成果一覧はメモリ careflow-rakusuke-rebranding / careflow-monitor-course-rows と
本ファイル§4.6系参照。R-1〜R-11すべて本番稼働済み（最終HEAD `96b825c`）。**

## 7. 2026-07-11 セッションでの消化状況（追記）

上記「その他の残タスク」①〜⑤と Cloudflare §6-3/6-4 を消化:
- **§6-3 アプリ内再ログイン導線 実装済み**: `lib/cfAccess.ts`（healthz 探針・throttle 30s）
  + `components/CloudflareAccessBanner.tsx`（全UI共通・Providers マウント）
  + `lib/api/fetcher.ts`（401/ネットワークエラー→探針→バナー。Access切れ時は signOut しない
  — /login 遷移も Access に弾かれるため）。復旧は window.location.reload() の
  **トップレベル遷移**（SW はナビゲーション network-only なのでキャッシュに阻まれない）。
- **§6-4 探針誤検知の根因調査**: novnc の `app/images/connect.svg` は**実在**
  （コンテナ直 curl で 200 / image/svg+xml、`/usr/share/novnc/app/images/` に確認）。
  誤検知は一過性のネットワーク/エッジ要因 → フェイルオープン化(96b825c)で対処済みが正。
- **①ESLint 警告ゼロ化**（未使用import・useMemo依存・import()型注釈は既存慣例の
  disable コメント方式） **②OfficeKey 型残骸削除** **④R-10b 文言規約を設計書へ追記**
  （rakusuke-advisor-ux-design.md） **⑤test_cleanup_w16 収集エラー修正**
  （_apply_fix→_apply_all_fixes の API 追随。3 pass）。
- **③根治**: reset-to-fixed のスタッフ選定を**コース担当優先**へ（courses.assigned_staff_id
  が有効ならそれを visit に採用・無効/未割当ならローテーション結果を**コース側へ書き戻し**
  = 乖離が構造的に残らない。auto_allocator_v2.py reset_visits_to_fixed）。
  assign-staff-only に**週単位の実行中ロック**（重複リクエストは 409・schedule.py。
  複数 worker 構成ではプロセス間非対応の注意コメントあり）。
  回帰テスト = tests/test_reset_course_staff_sync.py。
- **未消化（PO同席が必要）**: §6-1 Cloudflare ダッシュボードの Session Duration 延長、
  §6-2 Service Token 化。⑥フレークは据え置き。

## 8. 2026-07-12 セッション: 新人同行機能 全3フェーズ実装（追記）

**正典 = `docs/plans/trainee-accompaniment-design.md` v1.1**（PO要件確定→criticレビュー反映済み。
次のエージェントはまずこれ）。コミット: Phase 1 `6b9eaee`(mig 0060) / Phase 2 `362f9c1`(mig 0061) /
Phase 3 `c4cb1cb`。各フェーズ code-reviewer(opus) レビュー済み（CRITICAL/MAJOR 0 で着地）。

- **PO確定の運用モデル**: 紐付けの主体は新人（先輩のコース/患者に付く）・新人フラグON中はコースを
  持たない・患者の枠は1つのまま・カイポケへは職員名2に正規スタッフとして反映（同行○なし）
- **Phase 1**: `trainee_accompaniment_defaults`（毎週の既定）+`trainee_accompaniments`（週実効リンク・
  唯一の正典）。既定展開は週生成/固定枠に戻す/assign-staff-only の3地点で冪等実行。
  スケジュール「👥新人同行」モード（コース/患者の混在選択・時間重複は確定ブロック）。
  可視性3経路（一覧/単体GET/checkin）対応=新人本人のモバイル表示・打刻OK
- **Phase 2**: PATCH /courses の trainee 422 + FE候補除外。旧 staff_companion 機構を完全撤去
  （テーブルDROP・Layer3注入撤去・pending staff_mentor は410 graceful reject）。
  スタッフ詳細に同行サマリ・is_trainee OFF で今週以降リンク+既定削除（JST週境界）
- **Phase 3**: csv_builder の職員名2/3 解決を secondary→同行→mentor へ拡張（週次diffへ自然波及・
  Correction 2枠制限で3人目は週次では落ちる=月次では職員名3）。逆取込は同行新人集合への
  membership 判定で secondary書き戻し/要2名化をスキップ（ラウンドトリップ汚染防止）
- **RPA側は職員2欄対応済みを実査確認**（本番コンテナ `auto_apply.py` が `select#chargeStaff2Id1` 操作）
- **既知の残課題（軽微）**: ①連携ページ WeekDiffView は staff2 差分を表示しない（applyには載る・
  表示のみ未対応） ②要2名+同行の3人目は週次反映で落ちる（頻度を見て Correction staff3 拡張を判断）
  ③test_integration_kaipoke の3failは既存（audit middleware の AttributeError 起因・同行と無関係）
- **PO実機確認待ち**: 同行モードの操作感・新人モバイル表示・（Phase 3 は初回実applyで職員2反映を確認）

## 9. 2026-07-12 セッション: 新人同行の実運用フィードバック対応＋Cloudflare案内（追記）

**本番 HEAD = `defc284`（全コミットデプロイ済み・healthz内外OK・DB 0061のまま追加migrationなし）。**
§8の全3フェーズ実装後、POの実機フィードバックを受けて同日中に9回の改善デプロイを実施。
**設計書 `docs/plans/trainee-accompaniment-design.md` は都度更新済み＝同行の正典**（§8とセットで読む）。

### 9-1. やったこと（コミット順・すべて本番稼働）

| commit | 内容 |
|---|---|
| `5aabeaa` | **同行モードの操作が分からない問題**: 開始時に週/日タブを自動でタイムライン表示へ切替（選択操作はタイムラインのみ結線・一覧/リストのままだと何も押せなかった）＋モード中はリスト切替無効化＋下部バーに使い方ガイド常設 |
| `8015ed0` | **同住所×同時刻ペアを重複ブロックから免除**: 90分占有ルールの正当な同時刻のため（座標バケット.3f一致・FE/BE同一判定・座標なしは保守的にブロック） |
| `ff6bfb1` | **取込の新人staff2を自動同行化（案B・PO承認）**: staff2判定を3段階に（①既存リンク一致=スキップ ②リンク無し新人=同行リンク自動作成 ③実スタッフ=要2名化）。「要2名化すると盤面に2人目のコース列が無い」PO指摘への回答 |
| `878d78b` | 同行バッジをカード右上へ（患者名被り解消・ピンと共存）＋コース同行名をスタッフ名と同一行へ（ヘッダ縦ズレ根治） |
| `1d9195c` | 週リスト=極小👥アイコン・日リスト=👥名前チップで同行表示を拡張＋日タイムラインヘッダは👥アイコンのみ（select潰れ解消） |
| `3f06df6` | 週リストのコース同行を「担当: ◯◯」と同一行に名前つきで表示（PO要望） |
| `a46084d` | **★新人がコース担当になり得る3穴を封鎖**（PO依頼の調査で発見）: ①Layer3割付候補のis_trainee除外（**設計§8の「H8で除外済み」は誤認だった**——H8はコース数K/resetの除外でLayer3候補は未除外） ②inbound担当1の新人=edit不適用/add failed ③apply-staff-review 422。コスト関数不変=非新人の割付結果に影響なし |
| `defc284` | **複数名対応×同行のプール充足連携（PO提案）**: requires_multiple_staff患者の配置済み訪問がすべて同行つきなら②カード（2人目未配置）がプールから消える。同行モード中は②カードクリックで一括トグル。FEのみ（`accompanimentFulfillment.ts`純関数） |

### 9-2. 確定した運用ルール（POと合意・現場向け）

- **2人目が新人**: 「複数名対応」フラグON＋同行で充足（プールから消える・外れたら復活）
- **正規スタッフ2人が必要**: フラグON＋従来のプール経由ペア配置（slot0/1）
- **カイポケ**: 同行=職員名2に正規スタッフとして反映（同行○なし）。取込の新人staff2は自動同行化

### 9-3. 残タスク

- **（PO側ダッシュボード作業・未実施）** Cloudflare Access の Session Duration を 1 month へ
  （skyblue2025チーム・kaipoke-api/novncは同一アプリ=1箇所でOK・**ポリシー個別のSession Durationも要確認**）。
  1ヶ月超が欲しい場合は事業所固定IPの Bypass ポリシー追加。§6-1/6-2参照。手順は本セッションで案内済み
- **PO実機確認**: 同行モードの操作感（ガイドバー・プール②カードクリック）・同行つき週の初回カイポケ実apply（職員名2の反映確認・B系のPO監督原則に準ずる）
- バックログ: ①連携ページWeekDiffViewはstaff2差分を表示しない（applyには載る・表示のみ未対応）
  ②要2名+同行の3人目は週次反映で落ちる（Correction 2枠制限・月次CSVは職員名3で反映）
  ③pfv_validator V7警告（PFV編集画面の片肺warning）は同行充足を考慮しない（プールは考慮する・非対称）
  ④untracked 3ファイルの扱い未決（docs/HANDOFF.md=2026-06版で古い・renkei-layout-wireframe.html=§5参照物・CareLink-handoff.zip）

### 9-4. 気になる点（次のエージェント向け）★必読

1. **同行の正典は `trainee_accompaniments` テーブルのみ**。visits.*_staff_id/VSAには絶対書かない
   （読み出しはlive JOIN）。コース担当への書込経路を新設するときは必ず is_trainee ガードを入れる
2. **「固定枠に戻す」で患者個別の同行リンクは消える**（コースリンク＋毎週の既定は自動復元）。
   現場から「同行が消えた」と言われたらまずこれを疑う
3. **is_trainee OFF は今週以降の同行リンク+既定を削除**（確認ダイアログあり・過去週は履歴保持・JST週境界）
4. プールの充足判定は**保存済みリンクのみ**（モード中の未確定選択では消えない・確定PUTのinvalidateで反映）
5. 同行モードの選択UIは**タイムライン表示専用**（週/日リストはバッジ表示のみ）。モード開始で自動切替する
6. FEテスト基準値: **1185 pass / 0 fail**（D-11 BulkPoolInsertDialogのフルスイート限定フレークは既知・単体pass）。
   BE既存fail 17件はベースライン(0287a96)worktreeで同一再現を確認済み=環境起因
7. 本番にはまだ**新人フラグONのスタッフが居ない可能性**（同行UIはis_trainee=trueのactiveスタッフが
   居るときだけ出現）。現場開始時はスタッフ編集で新人フラグONから

## 10. 2026-07-12 セッション（続）: 自動スタッフ割当の4段ソルバ化（追記）

**正典 = `docs/plans/layer3-staged-mobilization-design.md` v1.1**（調査事実→PO確定3点→設計→レビュー履歴まで網羅。次のエージェントはまずこれ）。

### 10-1. 背景調査で確定した事実（本番DB実測・再調査不要）

- **過去9週で自動割当の結果が残っているのはW21/W27の2週のみ**。直近12日で assign 25回/unassign-all 12回の「ガチャ」運用実態
- **W28全解除の直接原因**: 7/12 17:25 の unassign-all が最後の操作（機能の失敗ではなくPO操作）
- **旧マネージャー救済はUUID昇順固定** → 熊澤（`70727ae8…`<川名`89b9aeb9…`）ばかり毎日1コース・川名は9週で1件
- **「前週同コードコード禁止」（Q3・曜日不問・週単位）が少人数拠点で数学的に破綻**: W27全埋め→W28で宇田川が全コード禁止（成功した週の翌週は必ず難航する振り子）
- **熊澤氏はアカウント=スタッフ/スタッフマスタ=マネージャーの不一致**（7/9 12:00のPATCHで設定）。クライアント設定のため**変更しない**（PO決定・ロジック側で対応）

### 10-2. 実装（4段ソルバ・全フェーズ完了）

- **Stage 1**: 正規スタッフのハンガリアン（既存・不変）
- **Stage 2**: 不足日のみマネージャー動員ハンガリアン（旧greedy `_apply_manager_fallback` は**完全撤去**。コスト関数を通るため川名/熊澤が履歴で自然交代・`via='manager_mobilized'`）
- **Stage 3**: 候補全滅時のみQ3をソフト化（`COST_ROTATION_RELAXED_VIOLATION=200_000`・性別/シフト/イベント/拠点はINFのまま・`via='rotation_relaxed'`）
- **Stage 4**: 既存レビューフロー不変
- レスポンス追加: `manager_mobilized_notices` / `rotation_relaxed_notices`（**committed のみ**。レビュー送り分は載せない）。FE=AssignWarningDialog に👔/🔁セクション＋auto_committed との重複はチップ併記
- テスト: BE 105 pass（W28実況再現 `test_w28_scenario_full_week_reproduction` 含む）/ FE 1197 pass / 0 fail
- レビュー履歴: critic（設計・MAJOR3件反映）→ディレクター（committed絞り込み指摘）→code-reviewer（CRITICAL/MAJOR 0・MINOR3件同日反映）

### 10-2b. v2.0（同日深夜）: PO要件訂正 → Q3全廃＋拠点跨ぎ救援 ★v1.1の記述を上書き

**PO明言（2026-07-12）: 「前週に担当したコースは翌週禁止」というルールは要件に存在しない**
（Q3ハード除外はレガシー。真の要件=「同じ患者さんを連続で担当するのを避けたい。拠点をまたいででも」のみ）。
正典 = 設計書 §11（v2.0・criticレビューREVISE→6 MAJOR反映済み）。

- **Q3ハード除外を全廃**（`ROTATION_EXCLUSION_WEEKS` 定数ごと削除・βの直近1週スキップも削除
  → weeks_ago=1 が最重 weight 1.0 でソフト減衰に乗る）。患者中心ローテ（1e6/5e5/2e5・
  DEPTH3・4週遡り）が連続回避の主役
- **旧Stage 3（ローテ緩和・rotation_relaxed）は撤去**（実装から数時間でPO要件訂正により廃止。
  本番 a695b1c には一時存在した）
- **新Stage 3 = 拠点跨ぎ救援 `_solve_stage3_cross_office`**: 未割当が残る日のみ当日を全再解
  （`COST_CROSS_OFFICE_VIOLATION=400_000`・スワップ許可・マネ含む）。採用ガード=
  「未割当数が厳密に減り、かつ既存カバーの退行なし」。性別/シフト/イベント/新人はINF聖域のまま
- 通知: `cross_office_notices`（🚗警告・所属→行き先拠点併記）/`rescue_swap_notices`
  （🔄報告・before→after）。committedのみ。FE基準値 **1199 pass / 0 fail** に更新
- **運用注意①**: 拠点別に順番に自動割当を実行すると先行拠点が他拠点スタッフを消費し得る。
  跨ぎ救援を期待する週は全拠点一括実行（office_id なし＝現行のスケジュール画面既定）を推奨
- **運用注意②（PO確認の余地）**: primary拠点が休業の曜日でも work_days が ON のスタッフは
  跨ぎ救援で呼び出され得る（機構上は妥当・運用意図は未確認）
- **効果**: W28で観測された「B=熊澤6連続・D=高岡固定」の元凶（Q3）が消え、日ごとの
  担当入れ替えは患者連続回避ペナルティが主導する。木曜都賀A（female_only×男性1名拠点）は
  再実行すればスワップ（例: 本名→稲毛コース・稲毛女性→都賀A）で自動充足し得る

### 10-3. セッション終了時点の状態と次のエージェントへの注意（2026-07-12深夜 最終更新）★必読

**本番 = v2.0（`2c93ef1`）稼働中・healthz内外OK・DB 0061のまま（本件migrationなし）。**
デプロイ2回実施: v1.1=`a695b1c`（20時台・POがW28で実割当し好感触＋質問2件）→ 同日中に
PO要件訂正で v2.0=`2c93ef1`（Q3全廃+跨ぎ救援）を実装・デプロイ。**POは v2.0 の実割当結果を
実機確認し「すごくいい」と高評価**（本セッションのゴール達成）。

1. **要件の正典（誤解厳禁）**: 割当ロジックの要件は「**同じ患者さんを連続で担当するのを避けたい
   （拠点をまたいででも）**」のみ。「前週同コース禁止」はPO要件に存在しない（Q3はレガシーで
   v2.0にて全廃済み）。新たなルールを足す前に必ずPOに要件確認すること
2. **PO確認待ち**: 患者・瀧本正芳さん（男性）の `sex_restriction=female_only` は入力ミスか
   意図か（W28木曜・都賀Aの未割当の根因。ミスなら患者編集で解除→本名さんが自動で入る。
   正しいなら跨ぎ救援のスワップで自動充足し得る）
3. **運用ルール**: 自動割当は全拠点一括実行を推奨（拠点別順次は先行拠点が他拠点スタッフを
   消費し得る・§10-2b）。primary休業日スタッフの跨ぎ呼び出しは機構上可能（運用意図はPO未確認）
4. **スタッフマスタの熊澤氏 role='manager' は触らない**（クライアント設定・PO決定。
   アカウント側=staffとの不一致は既知で、挙動はロジック側で吸収済み）
5. スコープ外（未着手のPO相談事項）: 不足バナー分母の見直し（manager/trainee除外のため
   稲毛は恒常不足表示）・assign-staff-onlyの非冪等性・週またぎ先読み（設計書§6）
6. テスト基準値: **FE 1199 pass / 0 fail・BE layer3系113 pass**。既存fail=
   `test_schedule_v2_api.py::test_reset_to_fixed_*` 2件（本改修前からの既知・layer3無関係）
7. 調査で確定した本番事実（§0.1・再調査不要）: 過去9週で割当が残ったのはW21/W27のみ・
   12日間でassign25回/unassign12回の反復運用だった→v2.0がこれを解消する狙い。
   スタッフ実構成=稲毛: staff2(宇田川/高岡)+manager2(川名/熊澤)+新人1(髙梨)、都賀: 本名(男性)1名
8. 残タスク（前セッションから継続）: Cloudflare Session Duration延長（PO側ダッシュボード作業・§9-3）・
   同行つき週の初回カイポケ実apply（PO監督）・untracked 3ファイルの扱い（§9-3④）

## 11. 2026-07-13 セッション: 訪問モニター地図の視認性改善（追記）

**本番 HEAD = `c7b2dc9` デプロイ済み・healthz内外OK・DB 0061のまま（frontendのみ・migration無し）。
PO実機確認済み「めっちゃいい」＝本件クローズ。**

### 11-1. 背景（PO報告）

モニター右端の地図で、①順路の指示線がブランドピンク（#e15a7f・太さ2・不透明度0.45）のため
**OSM背景の高速道路（ピンク系の塗り）と同化して不可視** ②番号マーカー（①②③…）が
「予定」状態=薄グレー `--status-future`（#d6d3d1）の16px丸に白9px文字で**ほぼ読めない**。

### 11-2. 修正内容（`c7b2dc9`・3ファイル）

| 対象 | 旧 | 新 |
|---|---|---|
| 順路線 | ピンク #e15a7f・w2・op0.45 | **紺青 `#2563eb`（`--map-route` 新設）・w3.5・op0.9 ＋ 白縁取り線 w7 の2層描画**（地図アプリ標準の見た目） |
| 番号マーカー | 16px丸・9px白文字 | **22px丸・12px太字白文字**・影強化。ピル型（同住所複数「1・2」）も同比率で拡大＋文字種別幅近似の係数更新 |
| マーカー色 | `STATUS_COLOR`（future=#d6d3d1/awaiting=#a8a29e が淡すぎ） | **地図専用 `MAP_MARKER_COLOR` を新設**（constants.ts）: future=`var(--text-secondary)`(#57534e・コントラスト約7:1)/awaiting=#78716c。**タイムライン等の面表示 STATUS_COLOR は不変** |
| 場所違いしきい値円 | ピンク・w1 | ティール `#0d9488`（--info）・w2（同じく道路と同化していたため） |

- 変更ファイル: `frontend/components/monitor/MonitorMapClient.tsx` / `components/monitor/constants.ts` / `styles/tokens.css`
- 検証: FE **1199 pass / 0 fail**（基準値どおり）・tsc/lint 警告0
- リブランディング時の「MonitorMapClient のSVG直書きは brand-primary と手動同期」（§4.6）は
  本件で**順路線・しきい値円がブランド色から独立**したため、以後の brand-primary 変更時の
  同期対象は 🏠 emojiIcon（`var(--brand-primary)` 使用・var可のため自動追従）のみ

### 11-3. 次のエージェントへ

- 色味の再調整依頼が来たら触るのは2箇所だけ: **`MAP_MARKER_COLOR`**（constants.ts・マーカー/ポップアップ）と
  **`--map-route`**（tokens.css。ただし Leaflet pathOptions は SVG属性で var() 不可のため
  MonitorMapClient.tsx 内の実値 `#2563eb` と手動同期が必要）
- 地図マーカーに新ステータスを足すときは STATUS_COLOR と MAP_MARKER_COLOR の**両方**に追加
  （淡色トークンをそのまま使うと白数字が読めない事故が再発する）
- 残タスクは §10-3 から変わらず: Cloudflare Session Duration延長（PO側）・同行つき週の
  初回カイポケ実apply（PO監督）・瀧本さん sex_restriction 確認・untracked 3ファイルの扱い

## 12. 2026-07-25〜26 セッション: カイポケイベント（個別業務）取り込み E-0〜E-3（追記）

**正典 = `docs/plans/kaipoke-event-inbound-design.md`**（PO要件確定→E-0実測→実装→レビューまで網羅。
次のエージェントはまずこれ）。メモリ = `careflow-kaipoke-individual-tasks.md`。

### 12-1. 何を作ったか

カイポケUIの緑色予定 = **「個別業務」**（面談/会議/休み/メモ等・職員単位）を、週末に楽スケへ
取り込む機能（週のバトンリレーのイベント版）。スケジュール表CSVには個別業務が構造的に
含まれない（実CSV529行全数+出力設定画面プローブで確定）ため、**職員スケジュール画面
（staffSchedule・週間）のスクレイピング**で取得する。

- **RPA（PlaywrightTest1・`b79f28d`）**: `/api/individual-tasks`。職員名「－」+currentDate週指定
  → `btnIndividual` 行をパース。実走検証: 7/25週23件（スクショと1対1一致）・7/13週27件
- **BE（`e876671`）**: mig **0062**（staff_events に source/external_id + 部分ユニーク索引）。
  `events_inbound.py` + `/integrations/events-inbound-preview|apply`（admin・ゲート=訪問と共有・
  dry_run既定・changesエコーバック+upsert意味論）。テスト10件
- **FE（`260136f`）**: 取り込みカードに**1ボタン統合**（❶訪問→イベント直列取得・❷2セクション
  プレビュー・❸直列適用・部分失敗でも他方続行）。テスト8件
- **メモチップ（`9b0222f`）**: ゼロ長イベント（00:00〜00:00メモ系）をタイムライン列上部に📝表示
- **レビュー**: code-reviewer CRITICAL/MAJOR 0・MINOR 5全反映（時刻pattern制限・Layer3ゼロ長
  guard・N+1解消・文言・タイムアウト）

### 12-2. 絶対に忘れてはいけない設計事実

1. **冪等キーは複合** `{個別業務ID}:{職員内部ID}:{YYYY-MM-DD}`。個別業務IDは週パターン由来の
   予定で**複数日同一**（宇田川7/25・7/28の休みが同一ID・実測）→ 単独IDをキーにすると2日目が消える
2. **手動イベント（source='manual'）には取り込みは決して触れない**（同行・訪問取り込みと同じ
   汚染防止原則）。取り込みが管理するのは source='kaipoke' の行だけ
3. 日曜分はRPAが返すがCareFlow側で除外（PO確定）。メモ系は start==end で保存（割当影響ゼロ）
4. ゲートは訪問取り込みと同一（**初回実applyまで全週無効**）
5. staff_events の手動編集API（PATCH）は start>=end を422で拒否 → 取り込んだメモを画面から
   編集すると保存できない（表示専用のチップにした理由。編集はカイポケ側で）

### 12-3. 運用開始条件と残タスク

- **★ゲート改訂（2026-07-26・PO確定）**: 「カイポケは請求と紐づく最終的な正。提供が始まる
  週は必ずカイポケに入力済み（④反映の有無は無関係）」とのPO判断で、取り込みゲートを
  **時間ベース**へ変更。`過去週・今週 = 無条件で取り込み可 / 未来週 = 実apply記録が必要`
  （`inbound_week_eligible`・訪問/イベント共通）。**初回実applyを待たずに運用開始できる**。
  安全弁として FE に大量キャンセル警告（候補>=10件で赤警告+曜日自動選択停止）を追加。
  未検証の注意点: ①実績(打刻)済み訪問と取り込みの衝突挙動 ②月跨ぎ週(訪問CSVは月単位取得の
  ため週後半の翌月分がキャンセル候補に見える可能性)。→ 初回の実データ取り込みで要確認
- 恒久変更（毎週続く担当替え等）はPFVへ手動反映が必要（取り込みは「その週限り」。
  毎週同じ差分が出る = PFVを直すサイン、という運用ルールをPOと合意済みの方針）
- 取得時間: ❶は訪問export(~50s)+イベント(~60-90s)の直列で**約2分**。Cloudflareの100s制限に
  イベント側が近い — 実運用で524が出たら export と同じ async+ポーリング化を検討（設計書§7）
- 残: 実データでの取り込みリハーサル（実apply後に❶→dry-run→取り込みの通し確認・PO立会い推奨）

## 5. 参照

- 設計原則・割当ソース: メモリ `careflow-staff-assignment-source.md`
- 共通パスワード運用: メモリ `careflow-unified-password.md`
- 前セッション（テーブル撤去・RBAC・PW締め付け・手順書）: `docs/plans/session-2026-07-08b-HANDOFF.md`
- 連携ページのレイアウト合意: `docs/mockups/renkei-layout-wireframe.html`
