# Runtime dependencies for CareLink backend.
# Use `pip install -r requirements.txt` (Docker image uses this).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
passlib[bcrypt]>=1.7.4,<2.0
# bcrypt 4.x raises ValueError on 72-byte test in passlib's detect_wrap_bug
# (passlib 1.7.4 was written before the bcrypt 4.x strict mode). Pin to 3.2.x.
bcrypt>=3.2,<4.0
python-jose[cryptography]>=3.3,<4.0
httpx>=0.27,<1.0
email-validator>=2.1,<3.0
python-multipart>=0.0.9
structlog>=24.1,<25.0
slowapi>=0.1.9,<1.0
openpyxl>=3.1,<4.0

# Dev / test (kept here so the image can run pytest without extras install).
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
aiosqlite>=0.20,<1.0
