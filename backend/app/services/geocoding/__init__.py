"""Geocoding service package — Phase 5-1 Wave 2-B revise.

Provides the address normalization + sha256 helpers used to populate
`geocoding_cache.address_hash` consistently across producers (Kaipoke
import, manual entry, etc.).
"""
